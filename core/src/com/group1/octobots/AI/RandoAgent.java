/*
package com.group1.octobots.AI;

import com.group1.octobots.Module;
import com.group1.octobots.physics.Movement;
import java.util.List;
import java.util.concurrent.ThreadLocalRandom;

*/
/**
 * Created on 5/21/2017.
 *//*

public class RandoAgent implements Agent {

    private Module target;

    public RandoAgent(Module target) {
        this.target = target;
    }

    @Override
    public Action act() {
        List<Move> moves = Movement.get().getAvailableMoves(target);
        return moves.get(ThreadLocalRandom.current().nextInt(moves.size()));
    }
}
*/
